<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alshamel.mall.mapper.AdjustmentReasonMapper">

    <select id="findAll" resultType="com.alshamel.mall.entity.AdjustmentReason">
        SELECT id, code, name, type, is_active AS active, created_at
        FROM adjustment_reason
        ORDER BY id
    </select>

    <select id="findAvailable" resultType="com.alshamel.mall.entity.AdjustmentReason">
        SELECT id, code, name, type, is_active AS active, created_at
        FROM adjustment_reason
        WHERE is_active = 1
          AND type = 'INVENTORY'
        ORDER BY id
    </select>

    <select id="findById" resultType="com.alshamel.mall.entity.AdjustmentReason">
        SELECT id, code, name, type, is_active AS active, created_at
        FROM adjustment_reason
        WHERE id = #{id}
    </select>

    <update id="updateActive">
        UPDATE adjustment_reason
        SET is_active = #{active}
        WHERE id = #{id}
    </update>

</mapper>
